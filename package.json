{
  "name": "mytheme-wp",
  "version": "0.1.0",
  "description": "Website - WordPress Theme",
  "repository": "",
  "author": "CAMP4 inc.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "engines": {
    "node": "16 || 18"
  },
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "prettier": "prettier --write --list-different .",
    "wp-setup": "zx ./bin/wp-setup.mjs",
    "wp-cli": "zx ./bin/wp-cli.mjs",
    "wp-wordmove": "zx ./bin/wp-wordmove.mjs"
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.19.3",
    "prettier": "^2.8.4",
    "prettier-plugin-css-order": "^1.3.0",
    "prettier-plugin-twig-melody": "^0.4.6",
    "zx": "^7.2.0"
  },
  "workspaces": [
    "themes/mytheme"
  ]
}
