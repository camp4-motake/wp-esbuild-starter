{
  "name": "mytheme",
  "author": "CAMP4 inc.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^18 || ^20"
  },
  "scripts": {
    "dev": "npm run reset && concurrently --raw npm:dev:*",
    "dev:scripts": "npm run esbuild -- --watch",
    "dev:styles": "npm run postcss -- --watch --map",
    "dev:images": "npm run images",
    "build": "npm run reset && concurrently --raw npm:build:*",
    "build:scripts": "tsc && npm run esbuild -- --minify",
    "build:styles": "NODE_ENV=production npm run postcss -- --no-map",
    "build:images": "npm run images",
    "esbuild": "esbuild src/{main,editor}.ts --bundle --outdir=dist --platform=browser --splitting --format=esm",
    "postcss": "postcss src/styles/{main,editor}.css -d dist --verbose",
    "images": "node image.config.mjs",
    "reset": "rimraf dist/*"
  },
  "devDependencies": {
    "@types/alpinejs": "^3.13.0",
    "@types/node": "^20.6.0",
    "@wordpress/browserslist-config": "^5.24.0",
    "concurrently": "^8.2.1",
    "cssnano": "^6.0.1",
    "csstype": "^3.1.2",
    "esbuild": "^0.19.3",
    "find-yarn-workspace-root": "^2.0.0",
    "fs-extra": "^11.1.1",
    "globby": "^13.2.2",
    "postcss": "^8.4.29",
    "postcss-cli": "^10.1.0",
    "postcss-easy-import": "^4.0.0",
    "postcss-preset-env": "^9.1.3",
    "postcss-sort-media-queries": "^5.2.0",
    "postcss-url": "^10.1.3",
    "rimraf": "^5.0.1",
    "sharp": "^0.32.5",
    "svgo": "3.0.2",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@alpinejs/intersect": "^3.13.0",
    "@wordpress/dom-ready": "^3.41.0",
    "alpinejs": "^3.13.0",
    "ress": "^5.0.2"
  },
  "browserslist": [
    "extends @wordpress/browserslist-config"
  ]
}
