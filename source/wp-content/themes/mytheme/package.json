{
  "name": "mytheme",
  "author": "CAMP4 inc.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^18 || ^20"
  },
  "scripts": {
    "scripts": "esbuild src/{main,editor}.ts --bundle --outdir=dist --platform=browser --splitting --format=esm",
    "styles": "postcss src/styles/{main,editor}.css -d dist --verbose",
    "dev": "npm-run-all -s clean -p dev:** --silent",
    "dev:scripts": "npm run scripts -- --watch",
    "dev:styles": "npm run styles -- --watch --map",
    "dev:images": "node image.config.mjs",
    "build": "npm-run-all -s clean -p build:** --silent",
    "build:scripts": "tsc && npm run scripts -- --minify",
    "build:styles": "NODE_ENV=production npm run styles -- --no-map",
    "build:images": "NODE_ENV=production node image.config.mjs",
    "clean": "rimraf dist/*"
  },
  "devDependencies": {
    "@types/alpinejs": "^3.13.5",
    "@types/node": "^20.10.2",
    "chokidar": "^3.5.3",
    "cssnano": "^6.0.1",
    "esbuild": "^0.19.8",
    "find-yarn-workspace-root": "^2.0.0",
    "fs-extra": "^11.2.0",
    "globby": "^14.0.0",
    "npm-run-all2": "^6.1.1",
    "postcss": "^8.4.32",
    "postcss-cli": "^10.1.0",
    "postcss-easy-import": "^4.0.0",
    "postcss-preset-env": "^9.3.0",
    "postcss-sort-media-queries": "^5.2.0",
    "postcss-url": "^10.1.3",
    "rimraf": "^5.0.5",
    "sharp": "^0.33.0",
    "svgo": "3.0.5",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "@alpinejs/intersect": "^3.13.3",
    "alpinejs": "^3.13.3",
    "budoux": "^0.6.1",
    "ress": "^5.0.2"
  },
  "browserslist": [
    "> 0.5% in JP and last 2 years and not dead"
  ]
}
